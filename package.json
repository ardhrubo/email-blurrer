{
  "name": "email-hider",
  "version": "1.1.0",
  "description": "A browser extension to hide emails on a page.",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "puppeteer": "^13.5.1"
  }
}
